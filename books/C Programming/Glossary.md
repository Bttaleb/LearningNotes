---
type: glossary
tags: [moc, glossary]
---

# Glossary

Every key term → the chapter section where it's defined or discussed. **This is the concept index.** Search here, then jump.

When you encounter a term in another chapter, add the new location below the existing one (`also: [[ChNN#section]]`). Backlinks will surface every place it's mentioned.

## A–C

- **conversion specification** — see [[Ch03 - Formatted Input-Output#Anatomy of a conversion specification]]
- **`%c` (char specifier)** — see [[Ch03 - Formatted Input-Output#scanf gotcha — the trailing newline]]

## D–F

- **`%d` / `%i`** — see [[Ch03 - Formatted Input-Output#%d — decimal integer]]
- **`%e` / `%E`** — see [[Ch03 - Formatted Input-Output#%e — exponential float]]
- **`%f`** — see [[Ch03 - Formatted Input-Output#%f — fixed decimal float]]
- **field width** — see [[Ch03 - Formatted Input-Output#Field width and precision]]
- **format string** — see [[Ch03 - Formatted Input-Output#Format strings]]

## G–I

- **`%g` / `%G`** — see [[Ch03 - Formatted Input-Output#%g — general float (smart)]]

## J–L

- **left-justify (`-` flag)** — see [[Ch03 - Formatted Input-Output#Justification]]

## M–P

- **precision** — see [[Ch03 - Formatted Input-Output#Field width and precision]]
- **`printf`** — see [[Ch03 - Formatted Input-Output#printf]]

## Q–S

- **`scanf`** — see [[Ch03 - Formatted Input-Output#scanf]]
- **`scanf` newline gotcha** — see [[Ch03 - Formatted Input-Output#scanf gotcha — the trailing newline]]

## T–Z

*(empty for now)*
